[build-system]
requires = ["setuptools>=64", "wheel", "torch", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "focal-w4a16"
version = "0.0.0"
description = "Small-M CUDA W4A16 linear benchmark"
requires-python = ">=3.10"
dependencies = ["torch", "pybind11"]
